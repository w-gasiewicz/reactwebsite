# This is simple React.js website with following functionalities:
1. User login/logout
2. VIN number decoder
3. Exchange rates table (values from https://api.ratesapi.io/api)
4. 3D world map (mapbox.com) 
